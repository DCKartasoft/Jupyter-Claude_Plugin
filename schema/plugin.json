{
  "jupyter.lab.shortcuts": [],
  "jupyter.lab.toolbars": {
    "Cell": [
      {
        "name": "jclaude-explain",
        "command": "jclaude:explain-cell",
        "rank": 60
      }
    ],
    "Notebook": [
      {
        "name": "jclaude-open-chat",
        "command": "jclaude:open-chat",
        "rank": 60
      }
    ]
  },
  "title": "Jupyter Claude",
  "description": "Settings for the Jupyter Claude collaboration extension.",
  "type": "object",
  "properties": {
    "backend": {
      "type": "string",
      "enum": ["anthropic", "bedrock"],
      "default": "anthropic",
      "title": "Backend",
      "description": "Which Claude backend to use. `anthropic` requires ANTHROPIC_API_KEY. `bedrock` uses the AWS credential chain."
    },
    "model": {
      "type": "string",
      "default": "claude-opus-4-8",
      "title": "Model",
      "description": "Model ID. Plain name for anthropic (e.g. `claude-opus-4-8`); cross-region inference profile for bedrock (e.g. `us.anthropic.claude-opus-4-8`)."
    },
    "awsRegion": {
      "type": "string",
      "default": "us-east-1",
      "title": "AWS region",
      "description": "AWS region for the Bedrock backend. Ignored when backend is `anthropic`."
    },
    "awsProfile": {
      "type": "string",
      "default": "",
      "title": "AWS profile",
      "description": "AWS named profile for the Bedrock backend (from ~/.aws/config). Uses shared SSO credentials when set. Empty means fall back to the Jupyter process's environment."
    },
    "defaultOpusModel": {
      "type": "string",
      "default": "us.anthropic.claude-opus-4-7",
      "title": "Default Opus model (Bedrock)",
      "description": "Bedrock inference-profile ID for the Opus tier."
    },
    "defaultSonnetModel": {
      "type": "string",
      "default": "us.anthropic.claude-sonnet-4-6",
      "title": "Default Sonnet model (Bedrock)",
      "description": "Bedrock inference-profile ID for the Sonnet tier."
    },
    "defaultHaikuModel": {
      "type": "string",
      "default": "us.anthropic.claude-haiku-4-5-20251001-v1:0",
      "title": "Default Haiku model (Bedrock)",
      "description": "Bedrock inference-profile ID for the Haiku tier."
    },
    "systemPrompt": {
      "type": "string",
      "default": "You are a helpful assistant collaborating with a data scientist inside a JupyterLab notebook.",
      "title": "System prompt",
      "description": "System prompt sent to Claude on every conversation."
    },
    "permissionMode": {
      "type": "string",
      "enum": ["default", "acceptEdits", "plan", "bypassPermissions"],
      "default": "default",
      "title": "Permission mode",
      "description": "How Claude asks for confirmation before using tools. `bypassPermissions` disables all checks (unsafe)."
    }
  },
  "additionalProperties": false
}
